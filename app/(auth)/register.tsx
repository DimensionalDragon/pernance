import { Text, View, SafeAreaView } from 'react-native';

export default function register() {
    return (
        <SafeAreaView>
          <View>
            <Text>Register</Text>
          </View>
        </SafeAreaView>
    );
}