import { Text, View, SafeAreaView } from 'react-native';

export default function login() {
    return (
        <SafeAreaView>
          <View>
            <Text>Login</Text>
          </View>
        </SafeAreaView>
    );
}